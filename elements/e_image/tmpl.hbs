<div class="e_image s_img">
    {{#if prop.imgList}}
    {{#each prop.imgList}}
        {{#eq @index 0}}
        {{#if prop.href.value}}
            <a href="{{$transferLink prop.href compId}}" target="{{prop.href.target}}">
        {{/if}}
        {{{nLazyImage imageUrl prop.title "{{isLazy}}"}}}
        {{#if prop.href.value}}
            </a>
        {{/if}}
        {{/eq}}
    {{/each}}
    {{^}}
    {{{nNoDataPrompt i18n.noData}}}
    {{/if}}
</div>