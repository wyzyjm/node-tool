<div class="e_bottomNav s_title" needjs="true">
    {{#if prop.dataList}}
        <!-- 导航内容 开始 -->
        <ul class="p_level1Box">
            {{#each prop.dataList}}
            <li class="p_level1Item">
                <!-- 名称 -->
                <p class="p_menu1Item s_templatetitle p_onu">
                    <a href="{{#if url}}{{url}}{{^}}javascript:;{{/if}}" target="{{#eq target 'blank'}}_blank{{/eq}}">
                        <span>{{navName}}</span>
                    </a>
                    {{#if children}}{{{../prop.arrow}}}{{/if}}
                </p>
                <!-- 子集 -->
                {{#if this.children}}
                <ul class="p_level2Box s_templatesum">
                    {{#each this.children}}
                    <li class="p_level2Item">
                        <!-- 名称 2 -->
                        <p class="p_menu2Item p_onu">
                            <a href="{{#if url}}{{url}}{{^}}javascript:;{{/if}}" target="{{#eq target 'blank'}}_blank{{/eq}}">
                                <span>{{navName}}</span>
                            </a>
                             {{#if this.children}}{{{../../prop.arrow}}}{{/if}}
                        </p>
                        <!-- 子集 2 -->
                        {{#if this.children}}
                        <ul class="p_level3Box">
                            {{#each this.children}}
                            <li class="p_level3Item">
                                <!-- 名称 3 -->
                                <p class="p_menu2Item">
                                    <a href="{{#if url}}{{url}}{{^}}javascript:;{{/if}}" target="{{#eq target 'blank'}}_blank{{/eq}}">
                                        <span>{{navName}}</span>
                                    </a>
                                </p>
                            </li>
                            {{/each}}
                        </ul>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
                {{/if}}
            </li>
            {{/each}}
        </ul>
        <!-- 导航内容 结束 -->
    {{^}}
        {{{nNoDataPrompt i18n.noData}}}
    {{/if}}
</div>

